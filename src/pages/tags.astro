---
import BaseLayout from "../layouts/BaseLayout.astro";
import KeyGroupedArticleList  from "../components/tags_categories/KeyGroupedArticleList.astro";
import {KeyType} from "../scripts/enums";

const title = 'Tags';
const description = 'Posts grouped by Tag.';
const permalink = `${Astro.site.href}${Astro.url.pathname}`;

---

<BaseLayout title={title} description={description} permalink={permalink} current="/tags"> <!-- {Astro.url.pathname} -->
    <KeyGroupedArticleList keyType={KeyType.Tags} />
</BaseLayout>
