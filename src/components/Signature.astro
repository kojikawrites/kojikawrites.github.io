---
import ThemedImage from './ThemedImage.astro'
import getSafeId from '../scripts/toSafeId';
import { getSiteCode } from "../scripts/getSiteConfig";
interface Props {
    signature: string
}

const { signature } = Astro.props
const id = getSafeId(signature, false);
const siteCode = getSiteCode();
const themed_src: Record<string, string> = {
    'dark': `/src/assets/images/${siteCode}/signatures/signature-${signature}-dark.png`,
    'light': `/src/assets/images/${siteCode}/signatures/signature-${signature}-light.png`
};

---

<!--<img alt={signature} src={img_src} />-->
<ThemedImage id={id} alt={signature}  srcList={themed_src} class="signature" />
