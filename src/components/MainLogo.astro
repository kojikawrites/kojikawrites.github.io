---
import SiteDatedLogo from 'src/components/SiteDatedLogo.astro';
import {getSiteConfig} from "src/scripts/getSiteConfig";

const siteConfig = await getSiteConfig();
---

<SiteDatedLogo alt={siteConfig.main.logo.alt}
          id="main-page-logo"
          class="main-page-logo"/>

