# Expose Astro dependencies for `pnpm` users
# shamefully-hoist=true

# Use lockfile version 3 for better cross-platform compatibility
# (npm 7+ format, more consistent between macOS and Linux)
lockfile-version=3

# Prevent platform-specific optional dependencies from causing lock file differences
# This helps keep lock files consistent across macOS, Linux, and Windows
install-strategy=hoisted
