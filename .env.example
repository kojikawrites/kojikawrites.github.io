# Site Configuration
# Copy this file to .env and customize for your environment

# Site code - domain name without protocol (e.g., hiivelabs.com, thewriteplace.rocks)
SITE_CODE=hiivelabs.com

# Default author for new blog posts in Keystatic editor
# This value pre-fills the author field when creating new posts
# Existing posts will retain their current author values
DEFAULT_AUTHOR=hiive

# Git Configuration (for Docker deployment)
# Used when committing changes from the /edit/deploy page
GIT_AUTHOR_NAME=hiive
GIT_AUTHOR_EMAIL=your-email@example.com
GIT_COMMITTER_NAME=hiive
GIT_COMMITTER_EMAIL=your-email@example.com

# Optional: GitHub Personal Access Token for HTTPS authentication
# Only needed if not using SSH keys
# Generate at: https://github.com/settings/tokens
# GITHUB_TOKEN=ghp_your_token_here
